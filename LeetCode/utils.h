#pragma once
#include "all_header.h"
#include <sstream>
#include <cstring>

using namespace std;

class Utils {

public:
	static vector<int> toVector(string array);
	static vector<string> split(string s, char index);
	
};